{%  extends "nlsnlsdbBundle:Default:bootstrap-template.html.twig" %}

{% block header %}
<div class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#">NewLeaf Symbiotics Knowledge Base</a>
      </div>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="nav-home nav-anchor active"><a href="#">Home</a></li>
          <li class="nav-anchor"><a href="#about">About</a></li>
          <li class="nav-anchor"><a href="#contact">Contact</a></li>
        </ul>
      </div>
    </div>
  </div>
{% endblock %}

{% block body %}
<!-- Wrap all page content here -->
<div id="wrap">

<section class="jumbotron text-center">
  <h1>Welcome to the NewLeaf Symbiotics Knowledge Base</h1>
  <p class="lead">Please interact with our data below</p>
</section>

<div class="container">
  <div class="row featurette">
    <div class="col-md-8">
      <h2 class="featurette-heading">NewLeaf Symbiotics knowledgebase. <span class="text-muted">Explore.</span></h2>
      <p class="lead">This repository is our portal to NewLeaf Symbiotics proprietary information and knowledge. Use it to mine data, find relationships, and understand our technology.</p>
    </div>
    <div class="col-md-4">
      <img src="/includes/images/ppfm.png" class="featurette-image img-responsive featurette-img" alt="PPFMs">
    </div>
  </div>
</div>

<div class="container">  
    <hr>  
    <div class="row">
        <div class="col-md-4">
          <div class="panel panel-default">
            <div class="panel-heading"><h3>Start with plant species.</h3></div>
            <div class="panel-body">Use this tool to interact with our data by first selecting a plant species. 
            </div>
            <div class="panel-footer text-right"><a href="#" class="btn btn-default">Go</a></div>
          </div>
        </div>
        <div class="col-md-4">
          <div class="panel panel-default">
            <div class="panel-heading"><h3>Start with PPFM strain.</h3></div>
            <div class="panel-body">Use this tool to interact with our data by first selecting a PPFM strain.
            </div>
            <div class="panel-footer text-right"><a href="#" class="btn btn-default">Go</a></div>
          </div>
        </div>
        <div class="col-md-4">
          <div class="panel panel-default">
            <div class="panel-heading"><h3>Start from scratch.</h3></div>
            <div class="panel-body">Use this tool to interact with our data starting from scratch.
            </div>
            <div class="panel-footer text-right"><a href="#" class="btn btn-default">Go</a></div>
          </div>
        </div>
    </div><!--/row-->  
    <hr>
</div><!--/container-->
</div>
</div>
{% endblock %}

{% block footer %}	
{% endblock %}
{% block javascript %}
<script type="text/javascript">
  $(document).ready(function() {

    // Parallax functionality
    (function($) {   
        $.fn.parallax = function(options) {
          var windowHeight = $(window).height();
          // Establish default settings
          var settings = $.extend({ speed : .15 }, options);
          // Iterate over each object in collection
          return this.each( function() {
            // Save a reference to the element
            var $this = $(this);
            // Set up Scroll Handler
            $(document).scroll(function(){
              var scrollTop = $(window).scrollTop();
              var offset = $this.offset().top;
              var height = $this.outerHeight();
              // Check if above or below viewport
              if (offset + height <= scrollTop || offset >= scrollTop + windowHeight) {
                return;
              }
              var yBgPosition = Math.round((offset - scrollTop - 50) * settings.speed);   
              // Apply the Y Background Position to Set the Parallax Effect
              $this.css('background-position', 'center ' + yBgPosition + 'px');
            });
          });
        }
      }(jQuery));
    
    // Apply parallax scrolling to jumbotron
    $('.jumbotron').parallax( { speed : 0.25 } );
    
    // Add onlick listeners to menu
    $(".nav-anchor").each(function() { 
      $(this).click(function() { 
        $(".nav-anchor").each(function() { $(this).removeClass("active"); });
        $(this).addClass("active");
      });
    });

    // Add onclick listener to home page link
    $('.navbar-brand').click(function() {
      $(".nav-anchor").each(function() { $(this).removeClass("active"); });
      $('.nav-home').addClass("active");
    });

  }); // Document ready
  
</script>
{% endblock %}
